#include<iostream>
#include<cstring>
#include<cstdio>
using namespace std;
int a[1000];
int b[1000];
//用于输入
char A[1000];
char B[1000];
void HP_and() {
	//高精度计算
	//用于计算
	//输入
	cin >> A >> B;
	//逆序存储
	int len = strlen(A);
	int len2 = strlen(B);
	for (int i = 0; i < len; i++) {
		a[i]=A[len-i-1]
	}
	
}