#include"БъЭЗ.h"
int main() {
	init();
	len = strlen(m);
	for (int i = 0; i <= 30;i++) {
		AND();
		if (check()==true) {
			cout << i+1;
			return 0;
		}
		
	}
	cout << "Impossible";
	return 0;
}