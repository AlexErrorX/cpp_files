#include"БъЭЗ.h"
int main() {
	
	/*int n;
	cin >> n;
	a[0] = 1;
	len = 1;
	for (int j = 0; j < n;j++) {
		chen(len);
	}
	for (int i = len - 1; i >= 0;i--) {
		cout << a[i];
	}*/
	return 0;
}